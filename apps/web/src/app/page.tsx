import { AGENT_PACKAGE } from "@novagait/agent";

export default function Home() {
  return (
    <main>
      <h1>Novagait Back Office</h1>
      <p>
        AP invoice intake with 3-way match: a production-grade agent
        demonstration with a published evaluation report, full audit trail, and
        human approval gates. Build in progress ({AGENT_PACKAGE} wired).
      </p>
    </main>
  );
}
